# Instagram-Dm-spammer
Instagram DM Responder i made for fun to troll my freind

A headless Python automation script that runs in the terminal to securely interact with a specific Instagram DM thread at a designated interval. Built using Selenium, it features human-like character typing emulation to ensure compatibility with modern JavaScript frameworks like React.

## Prerequisites

Ensure you have Firefox and Python installed on your system. 

```bash
# Example for Arch Linux users
sudo pacman -S python firefox
